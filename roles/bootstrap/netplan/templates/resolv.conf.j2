{{ ansible_managed |comment }}

{% for h in network_dns_servers -%}
nameserver {{ h }}
{% endfor -%}
{% if network_resolve_conf_search |length > 0 -%}
search {{ network_resolve_conf_search |join(' ') }}
{% endif -%}
{% if network_resolve_conf_options |length > 0 -%}
options {{ network_resolve_conf_options |join(' ') }}
{% endif -%}
